import BoiteModale from "./BoiteModale.js";

class BoiteModaleAlerte extends BoiteModale {
    constructor(conteneurHTML, contenu) {
        super(conteneurHTML, contenu);
    }

    alerter() {}
}

export default BoiteModaleAlerte;
