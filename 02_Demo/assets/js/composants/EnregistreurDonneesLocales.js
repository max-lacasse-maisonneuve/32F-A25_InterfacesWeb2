/**
 * Classe utilitaire pour enregistrer et récupérer des données dans le local storage
 */
export default class EnregistreurDonneesLocales {
    // Méthode statique qui enregistre des données dans le local storage
    // Peut être appelée sans instancier la classe
    static enregistrerDonneesLocales(cle, donnees) {}

    // Méthode statique qui récupère des données dans le local storage
    // Peut être appelée sans instancier la classe
    static recupererDonneesLocales(cle) {}

    // Méthode statique qui vide les données dans le local storage
    static viderDonneesLocales(cle) {}
}
